#include <iostream>
#include <cmath>

int main()
{
    double x = 3.14;
    double y = 4;
    double z;

    // z = std::max(x, y); Max number
    // z = std::min(x, y); Min number
    // z = pow(x, y); Power raised to the base number
    // z = sqrt(9); Square rote;
    // z = abs(-3); Absolute value
    // z = round(x); round number
    // z = ceil(x); round up
    // z = floor(x); round down

    std::cout << z;

    return 0;
}